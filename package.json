{
  "name": "roket",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint --ext ts src",
    "lint:fix": "eslint --ext .ts --fix src"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.0.2",
    "vite": "^5.4.11"
  }
}
